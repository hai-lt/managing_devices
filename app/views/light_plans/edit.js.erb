var modal = $('#simple_modal');
modal.find('.modal-body').html("<%= j render partial: 'form', object: @light_plan %>");
<% @title =  "#{ @light_plan.name }" %>
modal.find('.modal-title').html("<%= @title.html_safe %>");
modal.modal('show');
