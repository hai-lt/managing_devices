$("#simple_modal").find('.modal-body').html("<%= j render partial: 'lessons/form', object: @lesson  %>");
<% @title =  "#{ @lesson.new_record? ? "<h3>Create Lesson</h3>" : "<h3>Update Lesson</h3>" }" %>
$("#simple_modal").find('.modal-title').html("<%= @title.html_safe %>");
$("#simple_modal").modal('show');
